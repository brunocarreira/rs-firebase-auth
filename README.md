# rs-firebase-auth
Firebase Auth Library for Ride Sharing Project

This library contains classes used by Spring Security configuration in order to authenticate a request through Firebase token. It's used by all Ride Sharing services authenticated by Firebase.
